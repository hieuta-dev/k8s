apiVersion: batch/v1
kind: Job
metadata:
  name: pi
spec:
  template:
    spec:
      containers:
      - image: perl:5.34
        name: pi
        command: 
        - sh
        - -c
        - "perl -Mbignum=bpi -wle 'print bpi(2000)'"  
        resources: {}
      restartPolicy: Never
status: {}
