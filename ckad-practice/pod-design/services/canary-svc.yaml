apiVersion: v1
kind: Service
metadata:
  labels:
    app: canary-svc
  name: canary-svc
spec:
  ports:
  - name: nginx-port
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    tier: frontend
  type: ClusterIP
status:
  loadBalancer: {}
